# Wasabi 项目开发优化建议

这份文档聚焦于“这个主页项目接下来还可以怎么优化”，目标不是重写项目，而是在保留现有可用性的前提下，逐步提升可维护性、可扩展性和使用体验。

## 当前状态

- 1. 修复乱码和统一编码：已完成（2026-03-11 20:53:18 +08:00）
- 2. 给数据加校验：已完成（2026-03-11 20:53:18 +08:00）
- 3. 把 tab 配置改成数据驱动：已完成（2026-03-11 20:53:18 +08:00）
- 4. 把链接分组改成按分类 ID 映射：未完成
- 5. 拆分 docs/index.js 的职责：未完成
- 6. 明确技术路线（docs 或 React/TypeScript）：未完成
- 7. 增加导入数据能力：未完成
- 8. 增加搜索和快速定位：未完成
- 9. 优化移动端交互：未完成
- 10. 增加主页头部的个人信息区：未完成
## 总体判断

当前项目已经具备一个个人导航主页的核心能力：

- 有清晰的静态页面入口
- 有默认数据与本地缓存机制
- 有基础的新增、编辑、删除、导出能力
- 有可直接部署的 GitHub Pages 结构

但从开发和长期维护的角度看，项目还存在几个明显短板：

- 页面文案和编码历史问题较多
- 数据结构和页面结构耦合较深
- 缺少校验、测试和开发约束
- `docs/` 与 `src/` 出现双路线并存
- 一些功能能用，但维护成本偏高

如果后续准备继续长期维护，建议优先做“稳定性与可维护性”优化，再做“功能增强”和“视觉升级”。

## 优先级建议

建议按下面三个层次推进：

### P1：先解决维护成本高的问题

- 修复页面中的中文乱码和编码不一致问题
- 清理和统一数据结构约束
- 降低 HTML、JS、数据文件之间的硬编码耦合
- 补最基本的校验与异常处理

### P2：再补开发体验和可验证性

- 增加本地开发方式说明
- 增加数据校验脚本
- 增加基础测试或最小验证脚本
- 明确后续只维护 `docs/` 还是迁移到现代前端结构

### P3：最后做功能和体验升级

- 支持搜索、筛选、收藏高频入口
- 支持导入数据
- 优化移动端和视觉层次
- 提升主页的“个人主页”属性，而不只是书签页

## 具体优化建议

### 1. 修复编码问题

这是当前最应该优先处理的点。

现状：

- `docs/index.html`
- `docs/index.js`
- `docs/data.js`
- `.github/copilot-instructions.md`

这些文件里都能看到明显的中文乱码痕迹，说明项目历史上经历过编码混乱。

建议：

- 统一所有文本文件为 UTF-8
- 优先修复页面中用户可见的按钮、标题、表单文案
- 修复完成后再统一检查 README、开发文档和源码注释

收益：

- 页面更可读
- 维护时不容易误判字符串内容
- 后续继续扩展中文功能时更稳定

### 2. 把标签页配置从硬编码改为数据驱动

现状：

- HTML 里手写了两组 tabs
- `index.js` 的 `generateLinks()` 里手写了 `fragment-1`、`fragment-2`、`fragment-3`、`fragment-4`、`fragment-6`
- 分类数据里的 `page` 字段依赖这些固定值

问题：

- 新增 tab 时要同时改 HTML 和 JS
- 很容易出现“页面有 tab，但 JS 没渲染”或反过来的情况

建议：

- 新增一个 tab 配置对象或数组
- 由配置统一生成 tabs 结构和 fragment 列表
- `categories.page` 只引用配置中允许的 tab ID

收益：

- 新增或调整 tab 更简单
- 结构更清晰
- 减少硬编码遗漏

### 3. 给 `docs/data.js` 增加数据校验

现状：

- 当前数据依赖人工维护
- 代码默认假设分类名、链接对象、排序值都正确

风险：

- 分类名改了但 `initialLinks` 的键没同步
- `seq` 写错类型或留空
- `page` 指向了不存在的 fragment
- 链接缺少 `href`、`text`

建议：

- 增加一个本地校验脚本，检查：
  - 分类名是否唯一
  - 分类名是否都能在 `initialLinks` 中找到对应项
  - `page` 是否属于允许值
  - 链接字段是否完整
  - `seq` 是否可排序
- 在文档里写清楚数据修改后的检查步骤

收益：

- 降低手改数据带来的页面异常
- 让默认数据更可靠

### 4. 把链接分组从“按分类名映射”逐步改成“按分类 ID 映射”

现状：

- `initialLinks` 目前是按分类名称做键

问题：

- 分类重命名时需要同步迁移整个键名
- 名称既是展示文本又是数据索引，耦合过深

建议：

- 后续逐步迁移为按分类 ID 存储链接
- 展示层只读取分类名，不再把分类名当主索引
- 如果不想一次性重构，也可以先在内部逻辑层增加一层映射

收益：

- 分类改名更安全
- 数据结构更稳
- 后续支持多语言、别名、分组迁移会更容易

### 5. 拆分 `docs/index.js` 的职责

现状：

- 当前 `index.js` 集中了数据加载、渲染、事件绑定、表单处理、删除逻辑、弹窗逻辑

问题：

- 文件偏长
- 一个函数改动容易牵连其他逻辑
- 排查 bug 时上下文切换成本高

建议：

至少按职责拆成几块：

- 数据初始化
- 分类与链接渲染
- 弹窗与表单处理
- 数据增删改逻辑
- 通用工具函数

如果暂时不拆文件，至少先拆函数分区并统一命名风格。

收益：

- 降低维护成本
- 更容易定位问题
- 为以后迁移到模块化结构铺路

### 6. 明确技术路线：继续维护静态版，还是迁移到现代前端结构

现状：

- `docs/` 是实际运行代码
- `src/` 里有少量 React / TypeScript 文件，但未接入主流程

这说明项目现在有“双路线并存”的迹象。

建议二选一：

- 路线 A：彻底以 `docs/` 静态方案为主，移除或忽略 `src/` 残留实验代码
- 路线 B：明确计划迁移到 React / TypeScript，并补构建配置、入口、组件结构和发布流程

如果短期目标只是稳定维护主页，推荐先选路线 A。

收益：

- 避免后续维护时误判主入口
- 减少无效代码干扰
- 技术决策更清晰

### 7. 增加“导入数据”能力

现状：

- 当前已有导出 JS 数据能力
- 但没有对应的导入能力

问题：

- 页面内修改后的数据虽然能导出，但回写不方便
- 更换设备或浏览器时迁移成本高

建议：

- 支持导入导出的 JS 或 JSON 数据
- 导入前先做格式校验
- 导入时提供覆盖 / 合并两种模式

收益：

- 备份恢复更方便
- 多设备迁移更自然
- 真正形成闭环

### 8. 增加搜索和快速定位

现状：

- 链接数量已经不算少
- 当前完全依赖人工按分类找

建议：

- 增加顶部搜索框
- 支持按分类名、链接名、描述模糊匹配
- 搜索结果可直接跳转或高亮所属分类

收益：

- 链接越来越多时依然可用
- 大幅提高导航效率

### 9. 优化移动端交互

现状：

- 当前已有基础响应式样式
- 但核心交互仍偏桌面逻辑，比如双击编辑

问题：

- 移动端双击不够直观
- 表单操作密度较高

建议：

- 给分类和链接补明确的编辑入口按钮
- 移动端减少对双击事件的依赖
- 优化弹窗按钮布局和点击热区

收益：

- 手机上更好用
- 降低误操作

### 10. 增加主页头部的个人信息区

现状：

- 当前更像一个工具型导航页
- “个人主页”的身份感还不够强

建议：

- 在页头加入个人简介、头像、社交链接或一句简介
- 增加“常用 / 项目 / 关于我”之类的主页层级
- 把导航站和个人主页形象结合起来

收益：

- 仓库和主页会更完整
- 更适合对外展示

## 可立即执行的 5 个小改进

如果想先做投入小、收益高的改动，建议按这个顺序：

1. 修复 `docs/` 里的中文乱码文本
2. 给 `docs/data.js` 增加一份字段说明和维护规则
3. 提取 tab 配置，去掉 `generateLinks()` 里的 fragment 硬编码
4. 给页面增加搜索框
5. 明确 `docs/` 和 `src/` 的取舍，并更新文档

## 中期改造建议

如果准备把项目维护成一个长期使用的主页，可以考虑：

- 重构数据结构，按分类 ID 管理链接
- 模块化拆分 `index.js`
- 增加导入、搜索、置顶、高频入口等能力
- 增加基础测试和数据校验脚本

## 长期演进建议

如果未来希望它变成更完整的个人主页产品，可以考虑：

- 迁移到 TypeScript
- 建立统一的组件和页面结构
- 引入构建工具
- 支持主题配置、数据同步、跨设备使用
- 支持项目展示、文章入口、个人介绍等模块

## 建议的下一步

最推荐的下一步不是立刻重构，而是先做下面这三件事：

1. 修复乱码，统一编码
2. 补数据校验和维护规则
3. 把 tab 配置改成数据驱动

这样做成本不高，但会显著改善后面继续开发时的体验。
