# Wasabi 开发文档

这份文档面向后续维护这个主页代码的人，重点说明项目结构、运行方式、数据流、常见改动入口，以及目前需要注意的实现限制。

## 项目定位

这个仓库当前的可发布内容是一个纯静态个人导航主页，实际站点入口位于 `docs/` 目录。

项目没有发现 `package.json`、构建脚本或打包配置，因此目前不是一个依赖 npm / Vite / Webpack 的前端工程，而是一个可以直接通过静态文件运行的页面。

## 目录结构

```text
.
|-- README.md
|-- DEVELOPMENT.md
|-- docs/
|   |-- index.html
|   |-- index.js
|   |-- style.css
|   |-- data.js
|   |-- dataManager.js
|   |-- notyf-custom.css
|   |-- favicon.png
|   `-- index_files/
`-- src/
    |-- components/
    `-- utils/
```

### 关键文件说明

- `docs/index.html`
  - 页面主入口
  - 定义顶部按钮、两组标签页、链接编辑弹窗、分类编辑弹窗
  - 通过 CDN 引入 `jQuery`、`jQuery UI`、`Notyf`

- `docs/index.js`
  - 页面初始化主逻辑
  - 加载默认数据或本地缓存
  - 渲染分类与链接
  - 处理新增、编辑、删除、导出、清空缓存等交互

- `docs/style.css`
  - 整体视觉样式
  - 当前是深色、霓虹风格主页
  - 含响应式布局适配

- `docs/data.js`
  - 默认数据源
  - 定义 `initialCategories` 与 `initialLinks`
  - 修改它相当于修改页面的初始收藏内容

- `docs/dataManager.js`
  - `localStorage` 读写逻辑
  - 数据导出逻辑
  - 保存键名集中定义在这里

- `src/components/ClearStorage.tsx`
- `src/utils/storage.ts`
  - 这部分看起来像较早或试验性的 React/TS 代码
  - 当前站点主流程并未使用它们
  - 维护主页时应优先以 `docs/` 为准

## 运行与访问

### 线上访问

仓库命名和目录结构表明它用于 GitHub Pages 部署，实际页面应由 `docs/index.html` 提供。

### 本地查看

当前项目没有构建步骤，直接打开下面这个文件即可：

- `docs/index.html`

如果后续遇到浏览器对本地脚本或资源加载的限制，也可以用任意静态文件服务在仓库根目录启动本地服务，但这不是当前代码运行的前置条件。

## 页面结构

页面主体分成几个部分：

1. 顶部 Header
2. 主操作区按钮
3. 第一组标签页 `tabsEx1`
4. 第二组标签页 `tabsEx2`
5. 链接编辑弹窗 `#linkContent`
6. 分类编辑弹窗 `#editItemDiv`

当前标签页容器中实际使用的片段 ID 包括：

- `fragment-1`
- `fragment-2`
- `fragment-3`
- `fragment-4`
- `fragment-6`

如果以后新增标签页，需要同时更新 HTML 中的 tab 结构，以及 `index.js` 里 `generateLinks()` 使用的 `fragmentSelectors`。

## 数据模型

### 分类数据：`initialCategories`

这是一个数组，每一项代表一个分类对象：

```js
{
  id: "4",
  name: "分类名称",
  seq: 1,
  page: "fragment-1"
}
```

字段含义：

- `id`：分类唯一标识
- `name`：分类名，同时也是 `initialLinks` 的键
- `seq`：排序值，越小越靠前
- `page`：渲染到哪个 tab 面板

### 链接数据：`initialLinks`

这是一个对象，键是分类名称，值是该分类下的链接数组：

```js
{
  "分类名称": [
    {
      id: "10",
      href: "https://example.com",
      title: "补充说明",
      text: "显示名称",
      seq: "1"
    }
  ]
}
```

字段含义：

- `id`：链接唯一标识
- `href`：目标地址
- `title`：鼠标悬停或补充说明
- `text`：页面显示文本
- `seq`：排序值

## 数据加载与保存流程

页面启动后的大致流程如下：

1. `index.html` 先加载 `data.js`
2. 再加载 `dataManager.js`
3. 最后执行 `index.js`
4. `index.js` 在 `$(document).ready()` 中初始化应用
5. 优先从 `localStorage` 读取分类与链接
6. 如果本地没有可用数据，则复制 `data.js` 里的默认数据
7. 数据准备好后渲染分类和链接
8. 后续用户操作会持续回写到 `localStorage`

对应的本地存储键名目前是：

- `myBookmarks_categories_v5`
- `myBookmarks_links_v5`

这意味着只要浏览器本地已有缓存，`docs/data.js` 的修改不会立刻覆盖页面显示结果。

## 常见维护操作

### 1. 修改默认收藏内容

编辑：`docs/data.js`

适用场景：

- 新增常驻分类
- 删除不再需要的默认链接
- 调整默认排序
- 调整某个分类属于哪个 tab

注意事项：

- `initialLinks` 的键必须与分类 `name` 一致
- 修改分类名时，如果对应链接键名不一起改，页面会找不到该分类下的链接
- 页面已有本地缓存时，先清缓存再验证默认数据改动

### 2. 修改页面样式

编辑：`docs/style.css`

适用场景：

- 调整配色
- 修改按钮、分类标题、链接卡片样式
- 调整移动端布局

### 3. 修改交互逻辑

编辑：`docs/index.js`

适用场景：

- 调整新增 / 编辑 / 删除流程
- 修改双击交互行为
- 调整排序规则
- 修改 tab 渲染或弹窗行为

### 4. 修改本地存储或导出逻辑

编辑：`docs/dataManager.js`

适用场景：

- 变更 `localStorage` 键名
- 变更导出文件格式
- 增加加载失败时的兜底逻辑

## 交互说明

当前页面存在这些关键交互：

- 点击 `新增分類` 打开分类表单
- 点击 `新增連結` 打开链接表单
- 点击 `匯出資料 (JS)` 导出当前数据
- 点击 `清除本地存储` 清空缓存并刷新页面
- 双击分类标题，进入分类编辑
- 双击分类链接区域，进入该分类下的链接编辑流程

维护交互时，优先检查这些函数：

- `populateCategorySelect()`
- `generateLinks()`
- `bindEventHandlers()`
- `editLink()`
- `deleteLink()`
- `editCategory()`
- `deleteCategory()`
- `showModal()`
- `hideModal()`

## 已知实现特点与限制

### 1. `docs/` 才是当前主代码

`src/` 中虽然有 React / TypeScript 文件，但没有构建配置把它们接入到实际页面。若要继续维护现有主页，应把 `docs/` 视为主实现。

### 2. 分类名是链接映射的关键索引

`links` 对象是按分类名分组的，不是按分类 ID 分组。因此重命名分类时，必须同步迁移对应键名，否则会造成数据错位。

### 3. 页面内容高度依赖浏览器本地缓存

只要缓存存在，用户在页面中的改动就会覆盖默认数据。这对日常使用友好，但对开发调试不够直观。

### 4. 标签页 ID 写死在渲染逻辑里

`generateLinks()` 中有固定的 fragment 列表。如果 HTML 增减 tab 但忘了同步 JS，内容会渲染不全。

### 5. 当前仓库有编码历史问题

从现有源码内容看，部分中文文本存在历史编码混乱痕迹。后续如果要继续整理页面文案，建议统一按 UTF-8 编码保存，并逐步修正页面中仍然显示异常的中文字符串。

## 开发规则

后续每次进行功能开发、重构或修复时，必须遵守下面这条规则：

1. 先编写测试用例或明确验证用例
2. 先执行验证，确保测试用例能准确覆盖目标行为
3. 只有在验证方案明确后，才能开始开发和改动代码
4. 开发完成后必须再次执行验证
5. 如果验证不通过，不能直接保留改动，需要继续修复；若无法修复，则回退本次改动

这条规则适用于：

- 新功能开发
- Bug 修复
- 重构
- 数据结构调整
- 页面交互改动

简单说，默认流程必须是：先测试，后开发；验证通过，才保留改动。
## 推荐维护流程

当你要改这个主页时，建议按这个顺序：

1. 先确认是改默认数据、样式还是交互逻辑
2. 默认内容改 `docs/data.js`
3. 交互改 `docs/index.js`
4. 样式改 `docs/style.css`
5. 改完后清空浏览器本地存储再重新打开页面验证
6. 如需保留当前页面编辑结果，先用导出功能备份一份 JS 数据文件

## 验证清单

每次修改后至少检查下面几项：

- 页面是否能正常打开
- 两组标签页是否都能切换
- 分类是否按 `seq` 正确排序
- 链接是否按 `seq` 正确排序
- 新增分类后是否能立即渲染
- 新增链接后是否能立即渲染
- 编辑分类名后，原有链接是否仍然存在
- 删除分类后，对应链接是否一并删除
- 刷新页面后，本地修改是否还在
- 导出功能是否能正常下载文件

## 后续可选整理方向

如果后面继续演进这个项目，可以考虑：

- 修复页面中的中文乱码文本
- 为 `docs/data.js` 增加数据格式校验
- 把 tab 配置从写死改成数据驱动
- 增加导入功能，支持把导出的 JS 数据重新导回页面
- 统一 `docs/` 与 `src/` 的技术路线，避免双实现并存
